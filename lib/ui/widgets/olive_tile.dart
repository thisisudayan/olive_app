import 'package:flutter/material.dart';

class OliveTile extends StatelessWidget {
  const OliveTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}